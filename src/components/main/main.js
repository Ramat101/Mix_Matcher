import Logo from 'components/logo';
import Form from 'components/form';

import './main.scss';

function Main() {
    return (
        <main>
            <section>
                <Logo />
            </section>
            <section>
                <Form />
            </section>
        </main>

    );
}

export default Main;